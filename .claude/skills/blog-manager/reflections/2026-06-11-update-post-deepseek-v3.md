## 反思：2026-06-11 — 修改内容（文章重写）

### 任务
将原有的 DeepSeek 通用介绍文章重写为专门介绍 DeepSeek-V3 的技术文章，使用 agent-reach 搜索了 V3 的技术报告和资料。

### 做得好
- 使用 agent-reach 的 Exa 搜索获取了 DeepSeek-V3 技术报告的英文原文和中文解读，信息来源可靠
- 文章结构清晰地突出了 V3 相比 V2 的三项核心创新：无辅助损失负载均衡、MTP、FP8 训练
- 更新了 posts.json 中的日期和摘要，保持数据一致

### 可以改进
- Exa 搜索结果直接返回了技术报告的关键内容，但未能直接读取 arXiv 论文全文（Jina Reader 访问超时），可能需要尝试其他读取方式
- 对于模型性能的具体评测数据，可以进一步搜索以提供更详细的对比表格

### 改进措施
- 使用 agent-reach 的 agent-reach doctor 检查各渠道可用性后，优先选择响应最快的渠道进行搜索

> 注：本次反思中关于「同步 posts.json」的建议在 SKILL.md 中已有对应步骤（第 51 行），无需重复更新。

---

## 第二轮更新：2026-06-11 — 嵌入论文原图

### 任务
在 DeepSeek-V3 文章中嵌入从论文 PDF 中提取的 Figure 1（整体架构）和 Figure 2（MLA 架构），与已有的 Mermaid 示意图形成互补。

### 做得好
- 使用 PyMuPDF 将 PDF 页面渲染为 PNG（200 DPI，1654x2339），再用 Pillow 裁剪出 Figure 1（1494x1062）和 Figure 2（1494x1109），避免 ImageMagick 不可用的问题
- 论文原图贴在对应的 Mermaid 图下方，配有「论文原图」标识和双语说明，读者可以将 Mermaid 简化图与论文官方图对照理解
- 添加了 `.paper-figure` CSS 类，图片响应式缩放、带边框和圆角，与现有视觉风格一致
- 图片使用 `loading="lazy"` 延迟加载，不影响首屏渲染

### 可以改进
- 论文的架构图是矢量图形（TikZ 绘制），PyMuPDF 无法提取为独立 SVG/PDF，只能渲染全页 PNG 后裁剪，清晰度受限于 DPI 设置（当前 200 DPI）
- 后续如需更高清版本，可将 DPI 提升至 300+，但文件大小也会相应增加

### 改进措施
- 对于矢量绘图的论文，如果渲染 DPI 不够，可提高至 300 DPI 再裁剪；未来如需更多论文图可复用此流程

---

## 第四轮更新：2026-06-11 — 用户手动截图替代程序裁剪

### 任务
此前三次通过 Python 脚本（PyMuPDF + Pillow）从论文 PDF 裁剪的图像始终存在质量问题（偏移、包含多余文字），改用用户手动从 PDF 中截图的方式。

### 做得好
- 用户手动截图彻底解决了裁剪不精确的问题，图像质量远高于自动裁剪
- 新增了 FP8 训练框架图和模型综合性能对比图，丰富了文章的可视化内容
- 移除了不存在于论文中的"整体架构图"（用户指出论文中没有对应的独立架构图）
- 最终文章包含 4 张论文原图：MLA、MTP、FP8 训练框架、综合性能对比
- 图片统一使用 2 空格缩进格式，与项目代码风格一致

### 可以改进
- 手动截图需要用户参与，无法完全自动化；未来如需嵌入更多论文图，可先询问用户是否愿意手动截图
- 截图的分辨率和裁剪边界取决于用户操作精度，不同截图之间风格可能不一致

### 改进措施
- 对于关键论文图，优先让用户手动截图，避免反复尝试自动裁剪浪费时间
- 将截图文件统一放到 `assets/` 目录，命名规范 `paper-fig<N>-<description>.png`

### 做得好
- 通过 PyMuPDF 文本分析确认正确页面（Page 7 为架构图，Page 8 为 MLA 图，Page 10 为 MTP 图），而非从首页硬编码偏移
- 使用 300 DPI 渲染（比首次的 200 DPI 更高），裁剪得到的 PNG 更清晰：`paper-fig1-architecture.png`（2481×1333）、`paper-fig2-mla.png`（2481×1473）、`paper-fig3-mtp.png`（2481×1473）
- 裁剪坐标精确（top~38-42% of page），将标题栏和页面编号去除，仅保留图及图注
- 正确 PDF 文件复制到项目根目录备用
- 新增 Figure 3（MTP）嵌入文章 MTP 章节，与已有的 Mermaid 图形成互补

### 可以改进
- 不同版本的 PDF 页面排版可能存在差异（如 arXiv 的 LaTeX 更新），图的位置需重新确认
- 当前使用固定像素坐标裁剪，如需批量处理可考虑基于图像内容自动检测图边界

### 改进措施
- 提取论文图前先用 PyMuPDF 读取页面试探性文本，确认页面内容是否匹配预期
- 保留正确 PDF 副本到项目根目录，避免下次提取时找不到源文件